apiVersion: v1
kind: Service
metadata:
  name: sqlserver
spec:
  type: {{ .Values.service.type }}
  selector:
    app: sqlserver
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 1433
      protocol: TCP
      name: mssql
